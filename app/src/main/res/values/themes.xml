<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Light Theme -->
    <style name="Base.Theme.CryptoExchangeApp" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- App Theme -->
    <style name="Theme.CryptoExchangeApp" parent="Base.Theme.CryptoExchangeApp">
        <item name="colorSurface">@color/light_background</item>
        <item name="textColorPrimary">@color/light_text_primary</item>
        <item name="textColorSecondary">@color/light_text_secondary</item>
        <item name="ivIconColor">@color/light_icon_tint</item>
        <item name="cardBackgroundColor">@color/light_card_bg</item>
        <item name="gradientBackground">@drawable/bg_gradient_analytics_card_light</item>
        <item name="fabBgColor">@color/light_background</item>
    </style>

</resources>
